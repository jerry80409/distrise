debug: false
server:
  port: ${SERVER_PORT:8080}
  shutdown: graceful

spring:
  output:
    ansi:
      enabled: always
  jpa:
    open-in-view: false

logging:
  level:
    root: info
    rise.distrise.nostr: debug